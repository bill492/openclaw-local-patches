# OpenClaw Local Patches

All local OpenClaw override docs in this repo were retired on 2026-05-14 after upstream OpenClaw caught up.

This repository is intentionally kept as a tombstone so agents do not re-pull or reapply stale OV/WR content.

## Current status

- Active local dist patches: **none**
- Active local workaround docs: **none**
- Do not apply historical OV patches from old branches, archives, or session transcripts.
- If a regression appears, diagnose against current upstream first and create a fresh issue/PR instead of resurrecting an old carry.

## Retirement note

Retired by Bill at Bek's direction during the v2026.5.12 release audit. The old `docs/ov-*.md` files were deleted rather than marked active/resolved to avoid accidental reuse.
