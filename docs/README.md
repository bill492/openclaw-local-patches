# Retired OV docs

The local OpenClaw OV docs were deleted on 2026-05-14 because the local workaround stack is expired.

Do not reapply stale patches from memory, session logs, or old branches. Re-audit current upstream if a similar symptom returns.
